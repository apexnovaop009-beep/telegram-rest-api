PORT=3000
SERVER_NAME=server-1 # Must be unique for each server
APPLICATION_API_KEY= # This is used to authenticate requests ServerRoute

TELEGRAM_API_ID= # from https://my.telegram.org
TELEGRAM_API_HASH= # from https://my.telegram.org

# Remember to change the database provider in prisma/schema.prisma
DATABASE_URL=postgresql://your_db_user:your_db_password@db:5432/telegram_api

REDIS_URL=redis://redis:6379

# How often (in seconds) the session watchdog checks pool health
WATCHDOG_INTERVAL_SECONDS=10

# How often (in seconds) the queue job watchdog polls for pending jobs
QUEUE_JOB_INTERVAL_SECONDS=30

# Timezone used for scheduling (IANA format, e.g. Asia/Kuala_Lumpur, America/New_York)
APP_TIMEZONE=UTC

# Hour of day (0-23) in APP_TIMEZONE to run the media cleanup job
MEDIA_CLEANUP_HOUR=2

# How many days of media files to retain before deletion
MEDIA_RETENTION_DAYS=7

# Maximum file size (MB) to auto-download; documents/videos above this are skipped (photos exempt)
MEDIA_MAX_FILE_SIZE_MB=50

# Hard timeout (ms) per file download; partial files are deleted on timeout
MEDIA_DOWNLOAD_TIMEOUT_MS=60000
